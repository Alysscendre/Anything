// entry point for the application, import the app class from app.js and create a new instance + pass the root element 
import App from "./App.js";

const root = document.getElementById("app");
const app = new App(root);

